## 1 蜂鸣器驱动模块优化
### 故障现象
蜂鸣器鸣响时长不稳定；触发信号持续高电平时蜂鸣器会长鸣，无法按照预设时长关闭；外部信号毛刺偶尔造成蜂鸣器误触发。

### 排查猜想
1. 计数器计时阈值参数配置有误
2. 时钟分频时序存在误差

### 根因定位
原始代码采用**电平触发方式**：只要trig信号保持高电平，每个时钟周期都会重装计数器最大值。
当到站标志持续多个时钟周期为高，计数器不断被重置，无法完成倒计时。最终响铃时长等于触发信号高电平持续时间加上预设时长，表现为时长短不一；组合逻辑产生的毛刺也会造成误触发。

### 优化方案
将电平触发修改为上升沿检测，仅捕获单次有效脉冲启动倒计时；计时过程中屏蔽重复触发信号，避免计数器反复重载。

### 验证结果
Testbench覆盖长电平触发、窄毛刺、连续触发场景仿真；开发板上板验证，到站蜂鸣器可以固定时长鸣响，消除误触发、长时间鸣响问题。

---

## 2 呼梯调度模块 bug 排查
### 故障现象
电梯跨楼层运行时，偶尔目标楼层跳转异常；部分呼梯请求无法正常响应。

### 排查猜想
1. 楼层译码逻辑错误
2. 电梯上下行遍历判断顺序存在漏洞

### 根因定位
串行if-else优先级逻辑存在边界场景缺陷；呼叫锁存寄存器缺少清除逻辑，电梯到达目标楼层后，已完成的呼梯请求没有及时释放。

### 优化方案
完善上行、下行呼叫遍历判断分支，补齐边界楼层判断条件；增加到站清除逻辑，抵达目标楼层后释放对应的呼叫请求。

### 验证结果
搭建多组混合呼梯激励进行仿真测试；上板多次重复呼梯实验，所有呼叫指令均可正常响应，目标楼层计算稳定。

---

## 3 电梯主控 elevator_ctrl 逻辑调试
### 故障现象
仿真波形中beep_trig持续为0，到站无法触发蜂鸣器；开门自动关门时序在部分场景失效。

### 排查猜想
1. 有限状态机跳转条件缺失
2. 内部计时寄存器复位时机不合理

### 根因定位
两段式状态机内部计时器复位时序和状态跳转不同步；开门等待计时器仅在特定状态下启动，边界场景无法正常开始计数。

### 优化方案
调整计时器清零位置，保证进入开门状态后计时正常启动；梳理全部状态跳转分支，补齐缺失的时序逻辑。

### 验证结果
调整仿真激励完成波形验证；长时间上板运行测试，到站鸣笛、开门自动关门功能稳定。

---

## 4 串口通信 uart_mode_comm 模块优化
### 故障现象
上位机下发合法指令后，电梯无法回传状态信息；偶尔能够收到回复，但全部为乱码。

### 排查猜想
1. 波特率分频参数计算不准确，收发速率不匹配
2. 指令结束判定逻辑存在缺陷
3. 异步接收引脚存在亚稳态
4. 数据包发送时机冲突

### 根因定位
1. 初始分频参数存在误差，收发波特率不一致，硬件解码出现比特采样错误，产生乱码；
2. 仅依靠`\n`作为指令结束标志，缺少`\r`兼容识别，部分串口工具换行格式不匹配，模块判定不到指令结束，不会触发应答发送；
3. `uart_rx`为板外异步输入信号，无两级同步寄存器，存在亚稳态，接收字节偶尔出错；
4. 未等待单条字节发送完成就启动下一组数据包发送，造成发送FIFO数据错乱。

### 优化方案
1. 重新精确计算50MHz时钟下115200波特分频值，修正`BAUD_CNT_MAX`参数；
2. 同时识别`\r`、`\n`两种换行终止符，兼容各类串口调试工具；
3. 对uart_rx增加两级同步寄存器，规避跨时钟域亚稳态；
4. 增加状态机互斥控制：必须完整接收一条指令、接收状态回到IDLE后，才允许启动应答数据包发送，防止收发冲突。

### 验证结果
PC串口调试工具长时间下发指令测试；指令解析正常，每次收到指令稳定回传电梯楼层、方向、模式、门状态信息，乱码问题彻底解决。
